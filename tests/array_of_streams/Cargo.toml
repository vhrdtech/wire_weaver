[package]
name = "array_of_streams"
version = "0.1.0"
edition = "2024"

[dependencies]
wire_weaver.workspace = true
ww_client_server.workspace = true
tests_common = { path = "../tests_common" }
array_of_streams_api = { path = "../array_of_streams_api" }

[features]
default = ["std"]
std = []
